extends layout

block headercontent
	link(rel="stylesheet",href="/css/plugin/xjdailog.css",media="screen",type="text/css")
	style.
		body {
			padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
		}
block content
	h3 xjIframeDailog插件 使用样例，在页面中使用div 嵌套 iframe打开其他页面
	div
		div.alert.alert-info 
			| 调用xjIframeDailog之前需要在页面上引入对应的样式和脚本，因为xjIframeDailog依赖jquery，所以你还需额外引入jquery
		div.codep
			code
				|&lt;link rel="stylesheet" href="css/plugin/xjdailog.css" type="text/css"&gt;
		div.codep
			code
				|&lt;script src="js/plugin/xjiframedailog.js" type="text/javascript"&gt;&lt;/script&gt;
	h5 1. 带边框的dailog 默认值
		div(style="margin-top:20px;")
			button#btn1.btn.btn-primary(type="button") 打开模态框

block jscontent
	script(src="/js/plugin/xjiframedailog.js",type="text/javascript")
	script.
		var dailog = window.xjDailog ;
		$("#btn1").click(function(e){
			dailog.Open("dailog_i_1",{ width: 600,
								height: 500,
								caption: '自定义窗口标题',
								enabledrag:false ,//需要可以拖动的话 ，需要引入jquery.easyDrag;
								theme:"default", //默认主题
								onclose: null //窗口关闭时执行的回调函数  只有窗口通过函数主动关闭时才会触发。
						});
		});