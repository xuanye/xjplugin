<!DOCTYPE html>
<html>
	<head>
		<%include ../header%>
		<link rel="stylesheet" href="/css/plugin/xjdailog.css" media="screen" type="text/css"/>
	</head>
	<body>
		<%include ../toper%>
		<div class="container">
			<h3>xjIframeDailog插件 使用样例，在页面中使用div 嵌套 iframe打开其他页面</h3>
		    <div>
		      <div class="alert alert-info">
		         
		        调用xjIframeDailog之前需要在页面上引入对应的样式和脚本，因为xjIframeDailog依赖jquery，所以你还需额外引入jquery
		      </div>
		      <div class="codep"><code>&lt;link rel="stylesheet" href="css/plugin/xjdailog.css" type="text/css"&gt;</code></div>
		      <div class="codep"><code>&lt;script src="js/plugin/xjiframedailog.js" type="text/javascript"&gt;&lt;/script&gt;</code></div>
		    </div>
		    <h5>1. 带边框的dailog 默认值</h5>
		    <div style="margin-top:20px;">
		      <button id="btn1" type="button" class="btn btn-primary">打开模态框</button>
		    </div>
		    <h5>2. 无边框的dailog ，可以自行绘制样式（在内页中）</h5>
		    <div style="margin-top:20px;">
		      <button id="btn2" type="button" class="btn btn-primary">打开模态框2</button>
		    </div>
		</div>
		<%include ../bottomer%>
		<script src="/js/plugin/xjiframedailog.js" type="text/javascript"></script>
		<script type="text/javascript">
			var dailog = window.xjDailog ;
			$("#btn1").click(function(e){
				dailog.Open("dailog_i_1",{ width: 600,
									height: 500,
									caption: '自定义窗口标题',
									enabledrag:false ,//需要可以拖动的话 ，需要引入jquery.easyDrag;
									theme:"default", //默认主题
									onclose: function(userstate){alert("毁掉触发了:"+userstate.msg)} //窗口关闭时执行的回调函数  只有窗口通过函数主动关闭时才会触发。
							});
			});
			$("#btn2").click(function(e){
				dailog.Open("dailog_i_2",{ width: 600,
									height: 500,
									caption: '自定义窗口标题',
									enabledrag:false ,//需要可以拖动的话 ，需要引入jquery.easyDrag;
									theme:"simple", //默认主题
									onclose: function(userstate){alert("毁掉触发了:"+userstate.msg)} //窗口关闭时执行的回调函数  只有窗口通过函数主动关闭时才会触发。
							});
			});
		</script>
	</body>
</html>